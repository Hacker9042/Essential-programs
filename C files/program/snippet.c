#include<stdio.h>
int main()
{
    float a,b;
    printf("enter the first number:");
    scanf("%f", &a);
    printf("enter the second number:");
    scanf("%f", &b);
    printf("average of two number is: %f", (a+b)/2);
    return 0;
}

