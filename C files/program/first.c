#include<stdio.h>
int main()
{
    float a,b;
    printf("write the first number:");
    scanf("%f", &a);
    printf("write the second number:");
    scanf("%f", &b);
    printf("result: %f", a*b);
    return 0;
}